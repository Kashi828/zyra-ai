# ZYRA AI — Mission 90

## First-run configuration
- Added persistent non-secret product preferences.
- Added validated model, endpoint, voice, language, and telemetry configuration.
- Added setup configuration API.
- Kept legacy wizard `complete()` compatibility while the onboarding controller uses the new configuration-ready state.
- No credentials or tokens are stored in product configuration.
